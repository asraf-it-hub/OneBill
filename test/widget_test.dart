import 'package:flutter_test/flutter_test.dart';
import 'package:onebill/app/app.dart';

void main() {
  test('OneBill app root can be created', () {
    expect(const OneBillApp(), isA<OneBillApp>());
  });
}
